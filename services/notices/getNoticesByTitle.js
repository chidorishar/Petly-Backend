const { Notice, User } = require('../../models');
const { BadRequest } = require('http-errors');

const getNoticesByTitle = async (category, query, { skip, limit }, userId) => {
  
  
};

module.exports = getNoticesByTitle;
